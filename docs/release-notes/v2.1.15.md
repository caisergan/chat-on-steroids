## 2.1.15 — cos CLI and Claude Code plugin

- Add the `cos` command and an opt-in local control endpoint (Settings → CLI access, off by default) so a terminal or coding agent can send tasks, wait, follow, steer running turns, list models and read chats.
- Serve the `cos` operations as MCP tools with `cos mcp`, and answer Claude Code hooks for delegated ChatGPT work.
- Package the Claude Code integration as the `chat-on-steroids` plugin with delegation and orchestration skills and a ChatGPT worker agent.
- Add Search as a macOS ChatGPT browser, and start macOS Chrome in the background for background chats.
- Explain a queued message waiting behind a stalled turn and offer Stop turn.
- Keep sends, receipts and tool attribution working with ChatGPT's renamed composer, Chat/Work toggle and Markdown readback.
- Improve background worker rendering, alternate-shell request identity, recovery deadlines and Compact & Resume notices.
- Add Japanese interface translations.
- Update the bundled OpenAI tunnel-client to v0.0.15.

Reload the companion extension and refresh all CoS plugins in ChatGPT after updating.
